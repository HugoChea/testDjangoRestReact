# testDjangoRestReact

## How to launch the project
Run the API from root directory :
```
cd backend
docker-compose up -d

```

Run the API from root directory :
```
cd frontend
docker-compose up -d
```

Project should be available at :
http://localhost:3000/
